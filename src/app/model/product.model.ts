export interface Product{

  id:number,
  name:string,
  price:number,
  checked:boolean

}

// Ici on va creer la classe/interface qui represente un produit , pour eviter d'utiliser
// le type any(products:any not professional)
